version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: indent-db
    restart: always
    environment:
      POSTGRES_USER: indent_user
      POSTGRES_PASSWORD: indent_password
      POSTGRES_DB: indent_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4
    container_name: indent-pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@indentapp.local
      PGADMIN_DEFAULT_PASSWORD: adminpassword
    ports:
      - "5050:80"
    depends_on:
      - postgres

volumes:
  postgres_data:
